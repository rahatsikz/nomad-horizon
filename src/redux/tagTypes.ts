export enum tagTypes {
  user = "user",
  service = "service",
  booking = "booking",
  notification = "notification",
  schedule = "schedule",
  blog = "blog",
  event = "event",
  news = "news",
  review = "review",
}

export const tagTypesList = Object.values(tagTypes);
