export enum tagTypes {
  user = "user",
  service = "service",
  booking = "booking",
  notification = "notification",
  schedule = "schedule",
  blog = "blog",
}

export const tagTypesList = Object.values(tagTypes);
